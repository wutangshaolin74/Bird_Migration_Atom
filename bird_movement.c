#include <unistd.h>
#include <stdio.h>
#include <math.h>
#include <inttypes.h>
#include <stdlib.h>
#include <time.h>
#include <json-c/json.h>
#include <string.h>

void Bird_Movement(double lat_init, double long_init, /*destination*/) {
    /*so that bird don't start out of frame (our square of study)*/
    if (long_init < -160 || long_init > 120) || (lat_init < -15 || lat_init > 57) {
        printf("The initial position is out of frame")
    }
   
    while double isthebirdarrival(struct point  * currentposition, struct point * finalposition,double sizeofasquare) {
        
    }
}
   


    